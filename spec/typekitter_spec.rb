require 'spec_helper'

describe Typekitter do
  it 'has a version number' do
    expect(Typekitter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
